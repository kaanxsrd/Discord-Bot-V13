# Sıfır v13 Altyapı
İçerisin de Slash Commands / ContextMenu  sistemi mevcuttur.

Commands Handler, 
Events Handler, 
Slash Handler, 
Modals, 
ContextMenu, 
MongoDB

# KOMUT TASLAĞI
```
module.exports = {
  conf: {
    name: "",
    aliases: [],
    help: "",
    owner: true
  },

  run: async (client, message, args) => {

  },
};
```
